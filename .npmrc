# AuraScribe .npmrc
# For consistent dependency handling

# Use cache for faster installs
cache= ~/.npm-cache

# Trusted registries only
@aurascribe:registry=https://registry.npmjs.org/
# Optional: @scope with different registry
# @personal:registry=https://your-private-npm.com